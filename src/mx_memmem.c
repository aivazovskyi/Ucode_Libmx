#include "libmx.h"

void *mx_memmem(const void *big, size_t big_len, 
                const void *little, size_t little_len) {
    unsigned char *p_big;
    unsigned char *p_little;

    if (!big_len < little_len && (big_len && little_len) > 0) {
        p_big = (unsigned char *)big;
        p_little = (unsigned char *)
        

    }
    return NULL;
}
